Thank you for installing {{ .Chart.Name }}

Your release is named {{ .Release.Name }}

To retrieve the admin password use:

  $ kubectl get secrets/pihole-password --template={{ `{{.data.password}}`}} | base64 -d
